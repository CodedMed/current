<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/>
	</parent>

	<groupId>com.cashflowcopilot</groupId>
	<artifactId>ledger-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>ledger-service</name>
	<description>Cash Flow Copilot authoritative financial ledger service</description>

	<properties>
		<java.version>17</java.version>
		<embedded-postgres.version>2.2.2</embedded-postgres.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-jdbc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!--
		  Demo-mode only: lets `./mvnw spring-boot:run` boot with zero infrastructure by
		  starting a real PostgreSQL locally when DATABASE_URL is not configured.
		  The same Flyway migrations run verbatim. Disabled whenever DATABASE_URL is set.
		-->
		<dependency>
			<groupId>io.zonky.test</groupId>
			<artifactId>embedded-postgres</artifactId>
			<version>${embedded-postgres.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
			<!-- Single source of truth for SQL lives in the monorepo root `db/migrations`. -->
			<resource>
				<directory>${project.basedir}/../../db/migrations</directory>
				<targetPath>db/migration</targetPath>
			</resource>
			<!-- Tiger Data only. Added to spring.flyway.locations at runtime by
			     TimescaleFlywayConfig when the timescaledb extension is present. -->
			<resource>
				<directory>${project.basedir}/../../db/timescale</directory>
				<targetPath>db/timescale</targetPath>
			</resource>
			<!-- Demo seed fixtures live in the monorepo root `samples/seed`. -->
			<resource>
				<directory>${project.basedir}/../../samples/seed</directory>
				<targetPath>samples/seed</targetPath>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>
