# TimescaleDB refuses to create a continuous aggregate, and refuses to CALL
# refresh_continuous_aggregate, inside a transaction block. Flyway wraps every migration in one by
# default, so this migration opts out.
executeInTransaction=false
